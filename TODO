- scrape celu NRSR na prod, potom repairDatabase
	http://docs.mongodb.org/manual/reference/command/repairDatabase/#dbcmd.repairDatabase
	- overit pocet motions s len(text) > 1024 a pridat `hashed` na motions.text index u vs. databaz
		- update script:
			db.motions.dropIndex({"text": 1});
			db.motions.ensureIndex({"text": "hashed"});

- check Scrapy and Pupa
- create Session class?
- scraping of Speeches, ...
- scraping of other Person info
- historicki predsedovia parlamentu a rucne opravy clenstiev (historickych clenov vlad) podla Wikipedie?, resp. moznost rucnych oprav vseobecne
- ? parsers as a separate Flask application
